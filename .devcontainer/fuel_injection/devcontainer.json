{
  "name": "Boulder - Fuel injection with Gaussian pulse",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": "../.."
  },
  "forwardPorts": [
    8050
  ],
  "postCreateCommand": "conda env update -n boulder-examples -f environment.yml --prune",
  "postAttachCommand": "conda run -n boulder-examples boulder examples/fuel_injection.yaml --host 0.0.0.0 --no-open",
  "portsAttributes": {
    "8050": {
      "label": "Boulder - Fuel injection with Gaussian pulse",
      "onAutoForward": "openBrowser"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff"
      ]
    }
  },
  "remoteUser": "vscode"
}
